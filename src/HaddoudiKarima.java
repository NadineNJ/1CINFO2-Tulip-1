public class HaddoudiKarima {
    //Calculer la racine carrée d’un nombre
    public double calculRacineCarreeX(double x) {

        double racine = Math.sqrt(x);
        return racine;
    }
    //Convertir une chaine en majuscule

    public String convertirEnMajuscules(String texte) {
        return texte.toUpperCase();
    }

}
